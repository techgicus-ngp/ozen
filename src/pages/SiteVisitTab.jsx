// pages/SiteVisitTab.jsx
//
// Placeholder so EmployeeHome compiles today. Replace the body when you
// port site_visit_screen.dart; the `user` prop is already threaded through.

import React from 'react';
import { EmptyState } from '../components/HomeParts';

export default function SiteVisitTab() {
  return (
    <div className="tabpane">
      <EmptyState
        title="Site visits are coming here"
        body="Port site_visit_screen.dart into this component next."
      />
    </div>
  );
}
